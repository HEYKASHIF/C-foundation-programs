#include <stdio.h>
int main()
{
    int a,b,sum;
    printf("dear student anter two numbers for addition");
    scanf("%d%d" , &a,&b);
    sum=(a+b);
    printf("this is the addition of two numbers, %d" ,sum);
    return 0;
}