#include<stdio.h>
int main()
{
    int l,b,h;
    printf("Enter the length, breadth and height of a cuboid = ");
    scanf("%d%d%d",&l,&b,&h);
    printf("this is your volume of a cuboid = %d",l*b*h);
    return 0;
    
}